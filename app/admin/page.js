"use client";import{useState}from"react";export default function Admin(){const[u,setU]=useState(""),[p,setP]=useState(""),[m,setM]=useState("Ready to connect and import."),[busy,setBusy]=useState(false);async function go(e){e.preventDefault();setBusy(true);setM("Connecting to SMD and importing the catalogue...");try{const r=await fetch("/api/smd/sync",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({username:u,password:p})});const j=await r.json();if(!r.ok||!j.ok)throw Error(j.error||"Import failed");setM("Import complete. "+(j.products??0)+" SMD products synchronized. Storefront is now using the latest catalogue.");setP("");}catch(e){setM("Import failed: "+e.message)}finally{setBusy(false)}}return <div className="admin"><main className="adminmain"><section className="panel"><h1>GiftingGuru Admin</h1><p>SMD catalogue, reseller pricing and inventory management.</p><a href="/">← Storefront</a></section><section className="panel"><h2>SMD Technologies</h2><p>Connect once to securely save the reseller login and import products, prices, images and stock.</p><form onSubmit={go}><div className="field"><label>SMD reseller email / username</label><input required value={u} onChange={e=>setU(e.target.value)} disabled={busy}/></div><div className="field"><label>SMD password</label><input required type="password" value={p} onChange={e=>setP(e.target.value)} disabled={busy}/></div><button className="btn" disabled={busy}>{busy?"Importing SMD catalogue...":"Connect & Import Catalogue"}</button></form><div className="status">{m}</div></section><section className="panel"><h2>Retail pricing</h2><p>SMD excl-VAT price + 15% VAT, then 35% markup.</p></section></main></div>}